<%# locals: (project:) -%>
<%= turbo_stream.replace dom_id(project, :new_comment) do %>
  <%= turbo_frame_tag dom_id(project, :new_comment), src: new_project_comments_path(project_id: project.id) %>
<% end %>
